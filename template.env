## Template configuration for Risk Intelligence
#
# Copy this file to `.env` and fill in values.
#
# Notes:
# - If you use a remote cluster: set ARANGO_MODE=REMOTE and fill ARANGO_ENDPOINT/USERNAME/PASSWORD.
# - If you use local Docker: set ARANGO_MODE=LOCAL and fill LOCAL_ARANGO_* (or ARANGO_*).
# - When using self-managed Graph Analytics Engine (GRAL), the `/gral/...` routes can take ~30–90s
#   to become ready after the service is created. If you see transient 404/503s, wait/retry.

# Mode: REMOTE (cluster) or LOCAL (Docker)
ARANGO_MODE=REMOTE

# Remote cluster credentials (example includes :8529)
ARANGO_ENDPOINT=https://your-cluster.arango.ai:8529
ARANGO_USERNAME=root
ARANGO_PASSWORD=
ARANGO_DATABASE=risk-management

# Local Docker credentials (optional)
LOCAL_ARANGO_ENDPOINT=http://localhost:8529
LOCAL_ARANGO_USERNAME=root
LOCAL_ARANGO_PASSWORD=
LOCAL_ARANGO_DATABASE=risk-management

# Optional alias used by some scripts/tools
ARANGO_URL=

# GAE: use self-managed (JWT) unless AMP keys are provided
GAE_DEPLOYMENT_MODE=self_managed

# LLM (required for agentic workflow)
OPENROUTER_API_KEY=
OPENAI_API_KEY=

# Risk Analysis Config
RISK_ANALYSIS_ENABLE_CATALOG=true
RISK_ANALYSIS_PARALLELISM=false
RISK_ANALYSIS_MAX_EXECUTIONS=3
RISK_GRAPH_NAME=KnowledgeGraph
